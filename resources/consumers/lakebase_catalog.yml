resources:
  database_catalogs:
    docintel_state_catalog:
      name: ${var.schema}_state
      database_instance_name: ${var.lakebase_instance}
      database_name: ${var.lakebase_instance}
      create_database_if_not_exists: true
